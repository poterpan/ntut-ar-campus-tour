# 分工與模組介面

> W1 內定案。對應老師回饋「分工要明確」。

## 組員分工

| 代號 | 組員 | 負責模組 | 備註 |
|---|---|---|---|
| A | 潘柏嘉 | Geospatial AR 基礎 + POI 系統 + 最終整合 | 已完成 VPS 驗證、環境設定 |
| B | (待認領) | LLM 對話模組(API + 對話管理 + TTS) | 可在測試場景獨立開發 |
| C | (待認領) | 虛擬導遊 NPC(3D 模型、動畫、行為)+ POI 解說內容 | 模型/動畫可先做 |
| D | (待認領) | AR 校園貓(ML-Agents 訓練 + ONNX 整合) | 訓練需用 RTX 3090 |

> 蔡宗育、簡妤真、張凱琳 對應 B / C / D,依技能與意願於 W1 認領。

## 模組介面約定

W1 必須定案 —— 介面沒講清楚,平行開發會對不起來。

### POI 資料結構(待定)

範例欄位:

- `id` — string
- `name` — string
- `latitude` / `longitude` — double
- `anchorType` — Terrain / Rooftop / WGS84
- `description` — string(解說文本,見 `docs/POI.md`)

### NPC ↔ LLM 介面(待定)

範例:

- LLM 模組對外提供:`Task<string> AskAsync(string question, PoiContext context)`
- NPC 呼叫此介面取得回答,再交給 TTS 播放

### 校園貓 ONNX 規格(待定)

- 觀察空間(input tensor shape):……
- 動作空間(output):……
- 詳見 `docs/貓互動設計.md`

## W1 待決定清單

- [ ] B / C / D 三人組員認領
- [ ] POI 資料結構欄位定案
- [ ] NPC ↔ LLM 呼叫介面定案
- [ ] 校園貓 ONNX 輸入/輸出規格定案
- [ ] Unity 專案建立、Assets 結構、場景/prefab 策略(見 `CLAUDE.md`)
