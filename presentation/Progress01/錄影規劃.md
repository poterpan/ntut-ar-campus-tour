# Progress 01 — 錄影規劃

> 日期:2026-05-28 ｜ 預估長度:約 3 分鐘 ｜ 報告人:潘柏嘉

老師需求:確認我們有沒有開始動工、本週要看到 Unity Scene 出現。
策略:**iPhone 實機 demo 是「Unity Scene 出現」最強的證據**(實機跑的就是 Unity 編出來的 scene),配上 Editor 的程式碼/元件鏡頭補刀。

---

## 方案 A:整合版(~3 分鐘,iPhone demo 為主)

跟明天 POI 收集行程併行錄製,一趟出門兩件事辦完。

| 段落 | 內容 | 工具 |
|---|---|---|
| 0:00–0:15 開場 | 一句話講「W1 進度:Unity AR 骨架 + Geospatial + POI 收集工具」 | iPhone 前鏡頭 / 自拍 |
| 0:15–2:00 實機 demo(主菜) | 開 App → 相機 feed → 走到 POI(如新生南路側門)→ VPS lock ✅ → 按 Save POI → 綠色 toast 提示 → 走到下一個 POI 第二筆 → 列表上看得到 → 拍一下「📋 複製 JSON」按下去 | iPhone 內建螢幕錄影(控制中心) |
| 2:00–2:30 Unity Editor 一瞥 | Hierarchy 點 GeospatialController → Inspector 看到我們的 POICollector 元件 → 雙擊 POICollector.cs 切到 IDE 滾一遍 | Mac QuickTime 螢幕錄製 |
| 2:30–2:45 組員的貓模型 | 見下方〈組員的貓模型〉段 | 組員那邊錄 |
| 2:45–3:00 結尾 | 「明天收完 5 個 POI,下週進入 NPC + LLM 開發」 | 講話 |

---

## 方案 B:Unity Editor 為主(~60 秒硬證據版)

不出門、純螢幕錄製,把「我們真的在 Unity 裡做事」一次秀完。

| 段落 | 內容 |
|---|---|
| 0:00–0:15 | Hierarchy 點 GeospatialController → Inspector 出現 GeospatialController + **我們的 POICollector** 兩個元件並排(停 3 秒讓老師看清) |
| 0:15–0:40 | 切 Rider / VS Code / Cursor 開 POICollector.cs,從上往下滾一次(OnGUI、DoSave、CopyJsonToClipboard 都掃過) |
| 0:40–0:55 | Project 視窗點開 `Assets/Scripts/Geo/`、`Assets/Scripts/Editor/`;順手切 Project Settings → XR Plug-in Management → ARCore Extensions(看到 Geospatial ✓、iOS Support ✓) |
| 0:55–1:00 | 切瀏覽器 GitHub repo,顯示 commits 列表 + issue #3 + docs/ |

> **推薦做法**:方案 A 把方案 B 整段塞進中間「Unity Editor 一瞥」那塊,證據更扎實。

---

## 組員的貓模型(加進方案 A 或當獨立段)

組員(D / RL 訓練那位)的 Unity 專案已經匯入貓 3D 模型,可以拍 10–20 秒:

- **Scene 視窗用滑鼠中鍵旋轉模型** —— 展示 mesh / 材質
- 順手帶到 Project 視窗看 Assets 裡的 `.fbx` / textures / materials
- 若模型有附動畫,Animator 點兩個 state 切換給老師看

請組員用 **Mac QuickTime** 或 **iOS 螢幕錄影**(模型若先在他手機上)錄完,**AirDrop** 過來拼接。
口白可以一句:「校園貓模型已找好,接下來 D 組員會在獨立 ML-Agents 場景訓練。」

---

## Unity Editor 端可以秀的「我們真的在動工」證據清單

(供錄影時挑著用,不需全錄)

| 證據 | 怎麼找 |
|---|---|
| Inspector 看到自製元件 | Hierarchy 點 GeospatialController → 右邊 Inspector 滾下看 POICollector |
| C# 程式碼 | `Assets/Scripts/Geo/POICollector.cs`(~210 行)、`Assets/Scripts/Editor/POIBuildPostProcess.cs` |
| Project 視窗結構 | `Assets/Scripts/Geo/`、`Assets/Scripts/Editor/`、`Assets/Samples/ARCore Extensions/...`、`Assets/Settings/` |
| AR 配置 | Project Settings → XR Plug-in Management → iOS ARKit ✓ / Android ARCore ✓ |
| ARCore Extensions | Project Settings → XR Plug-in Management → ARCore Extensions:Geospatial ✓、iOS Support ✓、API Key 已填 |
| Build target | Build Profiles:iOS 是 active、Bundle ID `me.panspace.ntut-ar-campus-tour` |
| 套件清單 | Window → Package Manager:AR Foundation 6.3.4、ARCore Extensions 1.54.0 |
| GitHub 持續性 | repo 主頁 commit 列表 + issue #3 + docs/(分工、POI、貓互動設計) |

---

## 錄影前 Checklist

- [ ] iPhone **重 Build 一次**(才有 Files App File Sharing 生效 + POI Collector 最新 UI)
- [ ] iPhone 控制中心加「螢幕錄製」按鈕,長按它選「麥克風開啟」
- [ ] Unity Editor 預設 layout 確認 Hierarchy / Inspector / Project 都看得到;字級可以拉大一點讓鏡頭看得清
- [ ] 組員的貓模型片段 AirDrop 過來
- [ ] (可選)iMovie / DaVinci Resolve 串接,加幾條字幕點段落
- [ ] (可選)上傳 YouTube Unlisted 給老師連結

## 不用做的事

- 不用配樂、不用片頭片尾 motion graphics
- 不用打精緻字幕,YouTube 自動字幕能用就行
- 老師是確認有沒有動工,**不是評審影片品質**
